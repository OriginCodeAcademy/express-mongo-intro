'use strict';

module.exports = function(Todoitem) {
  // when a new item is created do the following
  // save, then post save query for the last item
  // update the todoitem to be a new integer + 1
};
